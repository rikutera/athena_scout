[phases.setup]
nixPkgs = ["nodejs"]
aptPkgs = []

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd backend && node server.js"
